// import router from '@/main/router/github'
// import { data, dataSteps } from './FunctionDataNavigationStep'

// // Function to update the current step based on the route
// export const updateCurrentStep = () => {
//   const currentPath = router.currentRoute.value.path
//   const index = data.value.Steps.findIndex((step) => step.route === currentPath)
//   if (index !== -1) {
//     data.value.currentStep = index
//   }
// }
// export { data, dataSteps }
